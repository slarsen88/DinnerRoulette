{
  "name": "backend",
  "version": "1.0.0",
  "description": "Tinder-esque app where users create a 'room' and swipe left or right on local restaurants. The first match will be the restaurant chosen.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4"
  }
}
